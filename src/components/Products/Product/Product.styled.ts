import styled from 'styled-components';

export const styles = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};

export const StyledProduct = styled.div`
  text-align: center;
`;
